# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/lkpg

# SMTP Configuration for Emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM=noreply@lankoping.se

# Base URL for email links (e.g., https://lankoping.se or http://localhost:3000)
BASE_URL=http://localhost:3000
